import Button from "../../components/Button/Button";

const StartPage = () => {
    return (
        <div>
            <Button>
                START
            </Button>

            <Button>
                SETTINGS
            </Button>

            <Button>
                RESULTS
            </Button>
        </div>
    );
};

export default StartPage;
